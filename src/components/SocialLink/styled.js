import styled from 'styled-components';

export const LinkContainer = styled.a`
  color: inherit;
  text-decoration: none;

  img {
    width: 40px;
    height: 40px;
    margin: 10px;
    cursor: pointer;
  }
`;
